# Environment Configuration Template
# Copy to .env and set actual values for deployment

# Application
VITE_APP_TITLE=CTP Susceptibility Map
VITE_APP_SUBTITLE=Community Transition Pathways

# Server (Node.js adapter)
HOST=0.0.0.0
PORT=3000

# CORS (for iframe embedding)
# Allow requests from specific domains
VITE_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Optional: MapLibre GL configuration
VITE_MAP_STYLE=https://demotiles.maplibre.org/style.json
